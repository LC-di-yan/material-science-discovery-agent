# 初赛提交材料清单（方向三 · 固态电解质）

> 对应《世界人工智能大赛说明.md》§5.1 / §03（算法赛题）初赛要求：
> **提交物：方案说明文档、技术路线概述**（必交）——说明所选方向、科学问题理解、技术方案与预期方法路线；**可附初步实验或可行性验证**。

## 本文件夹内容

| 文件                      | 对应赛题要求                   | 说明                                                              |
| ----------------------- | ------------------------ | --------------------------------------------------------------- |
| `初赛方案.pdf`（22 页） | **主提交物**：方案说明文档 + 技术路线概述 | 21 页正文 + 1 页附录（优化与修复增补说明摘要）；含阶段 I 最小闭环、阶段 II 扩充审计与阶段 III/IV（778 语料收官 + 自主发现规模化），覆盖选题、技术方案、路线 C 与可证伪验证计划 |
| `材料科学文献调研报告_Li6PS5Cl.md` | 方向三**基础任务报告** | 结构化 Gap、交叉引用、反证/假设收缩、路线优先级、局限与复现边界 |
| `data/` | 可附的**阶段性实验或可行性验证** | 可审计文献证据链、质量评估与路线库，支撑主方案的阶段性结果 |
| `调研报告_auto_20260815.md` | **基础任务报告**（确定性生成版） | `--stage report` 自 JSONL 渲染、无 LLM；九节全可回溯，证明报告非自由生成 |
| `优化与修复增补说明.md` | 修复与补齐记录 | P0 证据链修复 / P1 口径对齐 / P2 功能补齐的执行结果与验收口径 |
| `修复优化方案.md` | 修复记录与优化计划 | P0 证据链修复 / P1 口径对齐 / P2 功能补齐的执行方案与验收口径 |
| `detect_docid_mismatch.py`、`fix_docid.py` | 证据链自检/修复工具 | 检测并回填 gaps 的 `evidence_doc_ids` 与 records 真实 `doc_id` 的一致性 |

## data/ 证据链结构

> **本次扩充状态（2026-08-04）**：主数据集有 778 篇全文和 778 条成功抽取记录（remaining 0），全部全文已完成结构化抽取，无待重跑项；核心 argyrodite 记录 133/778（17.1%，evaluate 正则口径，已取代早期人工口径 33.3%）。知识层：Research Gap 19 个、候选假设 20 条、候选路线 12 条（route_A–L，其中 route_E–L 由 AgentScope 2.0 discover 循环自主产出）、逆向合成 12 条（retro_001–012）；`--stage audit` 判定 PASS。提交副本同步了结构化层、交叉验证、路线、对照审计与日志；全文语料保留为可追溯来源。闭源/商业 API 的使用范围、替代性与可复现性影响已在本节披露。

| 目录 | 内容 |
| --- | --- |
| `01_literature/sciverse/` | 检索/筛选/全文 JSON；778 篇全文以 `doc_id` 回溯；检索词表 v1–v4（queries/）。 |
| `02_knowledge/` | 结构化记录 778 条（`records.jsonl`）、Research Gap 19 个（`gaps.jsonl`，`evidence_doc_ids` 已按 record 反查回填修正，备份 `gaps.jsonl.pre_fixdocid`）、候选假设 20 条（`hypotheses.jsonl`）、10 条原文审计（`extraction_quality_audit_20260802.json`）、归一化电导率（`normalized_conductivity.jsonl`）与跨文献矛盾清单（`contradictions.jsonl`）。route_E–L / gap_012–019 / hyp_013–020 由 discover 循环自主产出。 |
| `03_cross_validate/` | Materials Project thermo/core 交叉验证；仅作为结构/热力学背景，不能验证合成路线。 |
| `04_routes/` | 候选路线 12 条（route_A–L）与逆向合成 12 条（`retrosynthesis.jsonl`/`retrosynthesis.md`）、可执行 DOE 协议（`experimental_DOE_protocols.md`）、逐步骤推理链（`reasoning_chains.md`）、`top3_routes.md` 包含扩充后的证据边界更新。 |
| `05_comparator/` | 统一对照协议四臂冻结路线（`routes_{arm}.jsonl`）与摘要；仅执行一次生成后冻结，审计阶段零网络零 LLM。 |
| `99_logs/` | 查询、抽取、验证、预算、本地质量评估、检索质量自评估（`retrieval_eval_20260815.json`，规则弱标注 P@K/Recall@K/nDCG@K）、知识层审计（`audit_20260804.json`，PASS）、四臂对照审计（`comparator_20260804.json`）与 discover 循环推理事件流（`discover_log.jsonl`），支持 `doc_id -> record_id` 回溯。 |
| `agent/README.md` | 运行入口、无密钥配置说明、API 边界、低并发恢复、本地评估与对照协议命令。 |
| `queries/` | 检索阶段查询词表（`expansion_v1.txt`），支撑 `agent run --stage search` 复跑。 |

## API 与复现披露

- **Sciverse**：用于论文检索、筛选和全文获取；可由同一公开 Sciverse 数据/API 在有效凭据下复跑。
- **LLM（deepseek-v4-flash 系列；当前配置端点 `tokenrhythm.studio/v1`、模型 `deepseek-v4-flash-0731`）**：仅用于从预先保存的全文片段提取结构化字段；预算日志记 663 次成功 LLM API 调用。抽取支持断点续跑，全部 778 篇全文已完成结构化。替代方案是本地/其他兼容 LLM 配合相同 JSON schema；模型差异会影响字段完整度，因此保留原始全文、record ID 和审计结果。
- **Materials Project API**：用于可公开复核的结构/热力学查询；本轮预算日志记 10 次调用。其缺失记录只表示数据库覆盖缺口，不作为材料稳定性或合成可行性的结论。
- **开源框架与运行时依赖**：数据/审计层（检索、筛选、抽取、验证、评估、对照审计）为自研 Python 标准库代码，零第三方依赖；认知层编排（`discover` 阶段）唯一第三方依赖为 **AgentScope 2.0**（Apache-2.0 许可，见 `requirements.txt`），已于 2026-08-03 验证可用（自主产出 `gap_012`/`hyp_013`/`route_E`，推理事件流存于 `discover_log.jsonl`）。替代方案：同一循环可改写为纯标准库调度循环，审计口径不变。

## 提交说明

- 本文件夹已排除敏感/内部文件：`_credentials.md`（API key）、`.mcp.json`、交接文档和开发环境配置。
- 每个新增科学主张均需回溯至 `record_id`、`doc_id` 及原始全文；高温电导率、理论值和引用他文数据不作为室温实验性能。
- `route_D` 的定位是建立可比性基线，不将文献性能外推为保证的实验结果。
