# 材料科学文献调研报告：Li6PS5Cl argyrodite 合成路线与工艺优化

> GOAI 世界人工智能开源大赛｜赛道三·算法赛题｜方向三·材料科学文献驱动的科学发现智能体｜路线 C
>
> 数据快照：§1–§10 主体为 2026-08-02（346 篇）；2026-08-04 扩充至 778 篇的最新口径见 §11。本报告只陈述可回溯至本项目 JSON/JSONL 证据链的结果；候选路线均为待证伪的实验假设，不是湿实验结果。

---

## 1. 摘要

本项目构建了一个面向硫化物 argyrodite `Li6PS5Cl` 的文献调研 Agent：从 Sciverse 检索、规则筛选和全文获取开始，以 LLM 将非结构化文献转为“体系—前驱体—工艺—性能—证据等级”记录，再进行 Gap 识别、候选工艺生成和 Materials Project（MP）结构/热力学背景查询。

截至本报告快照，系统保存 **346 篇全文**与 **346 条唯一 `doc_id -> record_id` 结构化记录**，全部全文已完成结构化抽取，无待重试项。局部原文审计显示：10 条抽查记录中 9 条可作直接核心体系证据、1 条只能作理论比较证据。最重要的结果不是单纯提出更多路线，而是扩充语料后识别出早期部分“未报道”表述过宽：Br-rich 无序/淬冷、氧硫化物保护层、富 Cl/Br 组成扫描和多价离子晶界调控已有直接先例。Agent 因此将这些方向从“强新颖性主张”收缩为有边界的待验证假设。

本报告最终优先给出可比性基线路线：对 `Li6PS5Cl` 建立压片烧结时间、相对密度、测试温度和 EIS 的统一记录框架；同时保留富氯组成特异热处理筛选等可证伪路线。该选择直接回应当前文献中“同配方、不同工艺/测量口径导致表观电导率不可比”的核心问题。

## 2. 研究问题与边界

### 2.1 科学问题

argyrodite 型硫化物电解质具有较高锂离子电导率，但面临空气/水分敏感、界面稳定性、合成能耗和跨文献性能不可比等问题。本项目聚焦：在不把文献性能简单拼接的前提下，如何从 `Li6PS5Cl` 及相邻 argyrodite 文献中提出**有明确文献依据、可复现实验验证、允许被否证**的合成与工艺假设。

### 2.2 Agent 的固定约束

- 性能字段必须回溯到 `record_id -> doc_id -> content JSON`；理论值、引用他文数值与实验测量值分开使用。
- 不将升温电导率记作室温电导率；不跨组成直接迁移最优热处理温度。
- MP 的条目或缺失条目只作为结构/热力学数据库背景，不能替代文献或实验的合成验证。
- 原始抽取记录维持追加式保存；后续审核以独立审计结果限制其使用范围，而非回写历史记录。

## 3. 系统与证据链

```text
Sciverse 检索/筛选 -> 全文 JSON -> LLM 结构化抽取 ->
records.jsonl -> Gap/假设/路线 -> 原文审计 + MP thermo/core -> 报告
```

- 检索、筛选、全文与日志：`data/01_literature/sciverse/`、`data/99_logs/`。
- 结构化知识层：`data/02_knowledge/records.jsonl`、`gaps.jsonl`、`hypotheses.jsonl`。
- 路线与交叉验证：`data/04_routes/candidate_routes.jsonl`、`data/03_cross_validate/`。
- 可重复的本地质量评估：`python -m agent.run --stage evaluate`，输出 `data/99_logs/evaluation_20260802.json`，不调用网络或 LLM。

## 4. 语料与抽取质量

### 4.1 数据规模

| 指标 | 结果 |
| --- | ---: |
| 已保存全文 | 346 篇 |
| 成功结构化记录 | 346 条 |
| 唯一 record ID | 346 |
| 唯一有全文来源的 doc ID | 346 |
| 尚未结构化全文 | 0 篇 |
| 核心 argyrodite 记录 | 88 / 346（25.4%） |
| 记录可回溯至本地全文 | 346 / 346（100%） |

数据来自 `data/99_logs/evaluation_20260802.json`。全部全文已完成结构化，无残留待重试项。

### 4.2 字段覆盖

| 字段 | 非空比例 |
| --- | ---: |
| 材料体系 | 81.2%（281/346） |
| 合成路线 | 58.1%（201/346） |
| 离子电导率 | 44.8%（155/346） |
| 前驱体 | 41.0%（142/346） |
| 掺杂信息 | 27.7%（96/346） |
| 球磨条件 | 25.1%（87/346） |
| 测量温度 | 26.3%（91/346） |
| 退火温度 | 21.1%（73/346） |
| 活化能 | 14.2%（49/346） |
| 空气稳定性 | 13.6%（47/346） |

字段缺失本身是发现：论文往往报告结果而不同时报告压实、热处理、温度与空气暴露条件，限制了直接的跨论文性能排序。

### 4.3 原文审计与抽取限制

对 10 条核心/相邻记录的原文核查保存在 `data/02_knowledge/extraction_quality_audit_20260802.json`。其中 `rec_067` 的 F 取代样品存在“原文给出完整球磨/热处理、结构化记录漏填关键字段”的情况，表明当前抽取适用于候选筛选而不是替代人工证据审查。

旧批次记录只有 49/346 保存了 `source_chunk`；但 346/346 都可从 `doc_id` 回溯到本地全文。抽取器已前向修复：未来成功记录保存实际发送给 LLM 的方法/性能片段。报告中的关键数值均以审计或原文证据约束，不把缺失字段自动补全。

## 5. 关键发现与反证

### 5.1 工艺可比性是当前最强、最可操作的 Gap

`gap_010` 聚焦同一 `Li6PS5Cl` 体系中压片/烧结形态、保温时间和测量温度造成的表观电导率差异。

- `rec_107`：550 C 下，压片烧结 4 h 样品报告 6.11 mS cm-1；粉末烧结更低。
- `rec_217`：500 rpm、24 h 球磨并在 500 C 处理 2 h 的样品，在 25 C 为 `1.30e-3 S cm-1`，在 60 C 为 `1.05e-2 S cm-1`。
- `rec_230`：`Li5.5PS4.5Cl1.5` 在 350 C 后报告 1.2 mS cm-1 和 0.385 eV，且该组成的最高电导率在 450 C 退火后出现。

这些证据说明：只有在组成、压实状态、热史和测试温度均被记录时，电导率比较才有解释意义。它比“再提出一个未检索充分的配方组合”更适合作为路线 C 的首个可验证任务。

### 5.2 反证推动假设收缩，而非事后维持过宽的新颖性

扩充语料对早期候选路线的边界进行了四项重要修正：

| 已有先例 | 直接证据 | 对路线选择的影响 |
| --- | --- | --- |
| Br-rich 无序与淬冷/慢冷对照 | `rec_111` | 不再宣称 Br-rich 无序或冷却控制本身未报道；任何新路线必须说明具体组成与变量组合。 |
| Li6PS5Cl 氧硫化物保护纳米层 | `rec_250` | 不再将“保护壳层”泛称为无先例；该论文报告约 50 nm 氧硫化物层，初始 3.02 mS cm-1，30 min 空气/浆料后仍高于 2.50 mS cm-1。 |
| 富 Cl/Br 组成扫描 | `rec_287` | 不再将整个 `Li5.5PS4.5ClxBr1.5-x` 空间称为未探索；记录指出电导率在约 x=0.8 附近最高。 |
| 多价阳离子晶界调控 | `rec_310` | 高值必须带温度标签：Ca 掺杂 12.9 mS cm-1 在 70 C，YCl3 改性 13 mS cm-1 在 50 C，不能写为室温增益。 |

该“反证—收缩”过程被记录为 `gap_011`。它是 Agent 的质量控制输出：系统不仅生成候选，也能在更大语料中找到会削弱其初始主张的证据。

### 5.3 保留的假设空间

- `hyp_011` / `route_D`：`Li6PS5Cl` 压片烧结时间 DOE，比较 2/4/6/8/10 h，并统一记录压力、相对密度、几何、XRD 和测量温度。它是方法学/可比性创新，不声称新组成。
- `hyp_012`：对同一 `Li5.5PS4.5Cl1.5` 前驱体比较 350 C 与 450 C 真空退火，联合报告相、密度、Ea 和室温 EIS。该路线采用同组成直接证据（`rec_230`），不把富 Br 的 430 C 窗口移植到富 Cl 体系。
- F 取代的工艺窗口（`hyp_003`）与混卤表面处理（`hyp_004`）保留为高风险候选：先需对精确组合进行新的逐篇检索，不能依赖“未见报道”的强断言。

## 6. 路线 C：可证伪的实验优先级

### 优先级 1：可比性基线路线（route_D）

**目标**：建立 `Li6PS5Cl` 的致密化—保温时间—测量温度对照，而非预设电导率提升。

1. 使用 `Li2S`、`P2S5`、`LiCl` 在干燥 Ar 条件下制备基线材料（`rec_107`）。
2. 在 550 C 下做压片烧结 2/4/6/8/10 h 时间序列，同时保留粉末烧结对照（`rec_107`）。
3. 每个样品记录压实压力、相对密度、样品几何、XRD 和预注册的室温/升温 EIS；升温结果单独标注（`rec_217`）。
4. 判定标准：同一测量口径下，是否能重现压片致密化带来的趋势；若不能，则将路线判为对文献条件高度敏感的负结果。

### 优先级 2：组成特异热处理验证（hyp_012）

**目标**：验证 `Li5.5PS4.5Cl1.5` 是否在同一前驱体和同一测量口径下呈现 350 C 与 450 C 的工艺差异。

- 支撑：`rec_230`。
- 失败也有价值：若 XRD、密度或室温 EIS 不支持文献趋势，应报告为组成/设备/压实敏感性，而不强行认定温度最优。

### 高风险探索路线

混卤直接退火、混卤 UDSH 表面处理、F 取代退火窗口均保留在 `candidate_routes.jsonl`，但当前应标为探索性假设。其路线可行性须由新的逐篇检索与实验共同判断，不以 MP 无条目替代验证。

## 7. Materials Project 交叉验证

MP baseline 中 `Li6PS5Cl`、`Li6PS5Br`、`Li6PS5I` 均有亚稳条目，现有记录的 `energy_above_hull` 范围约为 0.002–0.083 eV/atom。新增的 `Li6PS5F0.3Cl0.7`、`Li6PS5Cl0.75Br0.25` 和 `Li5.5PS4.5Cl1.5` thermo/core 查询均返回 0 条记录。

正确解释是：

- MP 条目可提供结构/热力学背景；
- 无条目是数据库覆盖缺口，不证明“新颖”、稳定或不稳定；
- MP 不应替代路线 C 所需的文献追溯、相分析和实验验证。

## 8. 评估协议与后续对照

已生成的 `evaluation_20260802.json` 是规则化证据质量基线，不是模型性能榜。按以下方法在同一文献库和同一审计规则下执行对照：

| 对照 | 操作 | 指标 |
| --- | --- | --- |
| 固定工艺 | 文献标准 Li6PS5Cl 流程 | 在完整元数据条件下的可复现性/可比较性 |
| 纯 LLM | 不提供检索证据，仅生成路线 | 有证据步骤比例、无证据断言率 |
| 随机拼接 | 随机组合已记录成分与条件 | 化学/证据有效率、违规率 |
| 文献 Agent | 检索、抽取、反证过滤后生成 | 同上，并增加”被反证后收缩率” |

**四组对照已实测（2026-08-03）**：在冻结语料（2026-08-02 快照）上运行 `python -m agent.run --stage gen --arm all --seed 42` 冻结各臂路线，再以 `python -m agent.run --stage compare` 做确定性审计。样本量：固定 1、纯 LLM 10、随机 20、文献 Agent 12（固定基线按参与规则只有组成/压实/热史/测量温度齐全的 `route_D` 合格，故维持 1 条）。结果见 `data/99_logs/comparator_20260803.json` 与 `data/05_comparator/comparator_summary.md`：文献 Agent 与固定基线全部步骤可回溯原文锚点（有证据步骤 100%、无证据断言 0%），且文献 Agent 具备反证后收缩率（17%）；纯 LLM 与随机基线按协议无证据锚点（先例命中高不等于证据可回溯，本轮纯 LLM 先例命中 60%、随机 50%，但均无证据锚点）；温度标签合规审计同时标记出文献 Agent 阶段 I 初始路线 `route_A/C` 的电导率断言缺测量温度标签（temp 0%），`route_D/B` 通过，正对应 `route_D` 的可比性口径改进；文献 Agent 的 `hyp_007` 氧化物壳层路线因目标组成只给定氧化物式（`Li4SiO4`，无 P/S）被约束审计标记。对照分数是对路线证据链与化学/先例合理性的审计，不构成性能优排行。

## 9. 复现、合规与局限

- 运行入口：见 `agent/README.md`；本地质量评估可运行 `python -m agent.run --stage evaluate`。
- 失败恢复：外部 LLM 稳定后可运行 `python -m agent.run --stage extract --workers 1`；已完成 `doc_id` 自动跳过。
- API：Sciverse 用于检索和全文，DeepSeek 用于结构化抽取，Materials Project 用于结构/热力学背景。调用范围、替代方案与可复现性影响见 `初赛提交材料/提交物清单.md`。
- 局限：当前未进行湿实验；性能只代表其原始文献条件；旧记录的证据片段字段不完整，须通过 `doc_id` 回溯原始全文。

## 10. 结论

本项目已经完成方向三基础任务的可审计闭环，并将路线 C 从“生成看似新颖的配方”收敛为“在严格证据边界下提出可证伪工艺假设”。扩充语料没有被用来夸大新颖性，而是帮助系统识别先例、收缩错误假设，并优先提出可比性基线。对固态电解质文献而言，这一能力直接降低了跨论文性能误判风险，也为后续湿实验筛选提供了更可信的起点。

## 11. 语料扩充更新（2026-08-04，778 快照）

> 本节为 §1–§10（2026-08-02 快照）的追加更新；规模与结论以本节为准，历史章节内容保留。

### 11.1 规模与口径

| 指标 | 结果 |
| --- | ---: |
| 已保存全文 | 778 篇 |
| 成功结构化记录（remaining 0） | 778 条 |
| 记录可回溯至本地全文 | 778 / 778（100%） |
| 核心 argyrodite 记录（evaluate 正则口径） | 133 / 778（17.1%） |
| Research Gap / 候选假设 / 候选路线 / 逆向合成 | 19 / 20 / 12 / 12 |
| 知识层审计 | `audit_20260804.json` 判定 PASS |

来源：`data/99_logs/evaluation_20260804.json`、`data/99_logs/audit_20260804.json`。

注：§4.1 的"核心 argyrodite 88/346=25.4%"为 08-02 人工口径；统一后的机器口径为 `evaluate` 正则算得的 133/778=17.1%，本文后续以该机器口径为准。

### 11.2 自主发现产出（discover 循环）

- `gap_012–019`、`hyp_013–020`、`route_E–L` 由 AgentScope 2.0 discover 循环自主产出（route_E–L 覆盖 Ca/Y 共掺、Se 取代、O 取代、溶液法、ALD 包覆、I 基取代、Sb 基高熵、热压），推理事件流见 `data/99_logs/discover_log.jsonl`。
- 循环以轮次上限收敛（2/3/5 轮共 3 次运行，`stop_reason` 均为"达到最大轮次"，"轮内零新增"为兜底条件）。

### 11.3 四臂对照（2026-08-04 审计）

| 生成臂 | 路线数 | 有证据步骤 | 无证据断言率 | 温度标签合规 | 约束通过率 | 先例命中 | 反证收缩率 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 固定工艺基线 | 1 | 100% | 0% | 100% | 100% | 50% | 0% |
| 纯 LLM 基线 | 10 | 0% | 100% | 100% | 100% | 63% | 0% |
| 随机拼接基线 | 20 | 0% | 100% | 52% | 84% | 50% | 0% |
| 文献 Agent | 12 | 100% | 0% | 83% | 98% | 17% | 10% |

来源：`data/99_logs/comparator_20260804.json`。对照输出是文献证据链与化学/先例合理性的审计，不构成湿实验性能或模型间排行。

### 11.4 证据链修复（可复现）

- 扩充审计发现：discover 生成的 `gap_012–019` 的 `evidence_doc_ids` 存在复用/伪造（与 `record_id` 反查的真实 `doc_id` 不一致；连同早期 `gap_001–009` 的 12 位短哈希截断，修复前共 56 处 mismatch）。
- 修复：`fix_docid.py` 按 `record_id` 反查回填全部真实 `doc_id`（原始件备份为 `gaps.jsonl.pre_fixdocid`）；`write_gap` 改为落盘前按 `record_id` 回填、不信任调用方传入的 `doc_id`；`audit.py` 新增 `record_id↔doc_id` 配对一致性校验。
- 验收：`detect_docid_mismatch.py` mismatch = 0；`audit` PASS 且该项校验可复跑复现。

### 11.5 单位归一化与系统化矛盾检测

**电导率单位归一化**（`python -m agent.run --stage normalize` → `data/02_knowledge/normalized_conductivity.jsonl`）：把 283 种非结构化 `conductivity` 写法规一为统一单位 S/cm 数值。确定性解析器覆盖科学计数、`×10⁻³` 上标、LaTeX `10^{-3}`、`mS/cm↔S/cm` 换算、缺乘号 `3 10-4`、不确定度 `3.0(2)`；对范围/多值/纯描述一律标记、不猜测数值。结果：可归一单值 **188** 条（值域 1e-9 ~ 2e-1 S/cm，中位 1 mS/cm），multi 68、geq 12、approx 13、unparseable 23、empty 473。字段缺失与描述性文本占比本身即说明"抽取层数值规范化"缺口。

**系统化跨文献矛盾检测**（`python -m agent.run --stage contradictions` → `data/02_knowledge/contradictions.jsonl`）：对"同一归一体系内、电导率相差 ≥10 倍"的记录对做确定性成对检测（排除 <1e-7 S/cm 的电子电导/噪声），每条保留双方 `measurement_temp` 供复核。结果：**85 条候选矛盾对、17 个归一体系参与**，top 集中在 Li6PS5Cl 体系内跨文献 2–3 个数量级分散（如 rec_187 `2.66e-2` vs rec_763 `1e-5` S/cm，×2660）。这条把原 `gap_005` 的定性判断"同配方跨文献相差一个数量级"量化为可追溯至 `record_id/doc_id` 的清单。

**自动报告生成**（`python -m agent.run --stage report --snapshot 20260804` → `调研报告_auto_20260804.md`）：从本地 JSON/JSONL 确定性渲染报告（无 LLM、无网络），含语料规模、字段覆盖、归一化统计、Gap/假设/路线、矛盾清单、四臂对照与审计结论九节，证明"报告不是 LLM 自由发挥"。
