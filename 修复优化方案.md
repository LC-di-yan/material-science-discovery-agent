# 修复优化方案（含执行状态）

> 本方案记录对初赛提交快照（2026-08-04）的修复与优化计划，以及各条的执行状态。
> 执行口径：**纠正而非掩盖**——发现问题先留档、再修复、再让审计能复现抓取，绝不删除证据、绝不削弱审计以强凑 PASS、绝不虚构结论。

## 修复原则与红线

1. 证据链任何不一致，先备份留痕，再修复；修复脚本与验收脚本保留，使复核可复现。
2. 不删除历史数据，只做追加式修改 + 明确标注备份。
3. 不为了 PASS 而放宽 audit 校验；相反，把"审计漏掉的问题"补进 audit。
4. 所有新增功能为纯标准库确定性实现，不引入第三方依赖，不依赖网络与 LLM。

## P0 · 证据链正确性修复 —— 已执行 ✅

**发现问题**：discover 循环自主产出的 `gap_012–019` 的 `evidence_doc_ids` 被复用于多个不同 `record_id`（伪造为同一 `dccf…0de3`），连同早期 `gap_001–009` 的 12 位短哈希截断，共 **56 处 mismatch**。

**根因**：`read_knowledge` 不返回 `doc_id` → LLM 臆造；`write_gap` 不回填不校验；`audit` 不核对 doc_id 配对。

**修复**：`fix_docid.py` 反查回填 17 个 gap；`tools.py` 暴露 `doc_id` + 落盘回填；`audit.py` 新增配对校验。验收：mismatch = 0，audit PASS。

## P1 · 口径与可复现对齐 —— 已执行 ✅

- evaluate 快照日期化 + 冻结保护；comparator 冻结拒绝覆盖；
- 统一核心 argyrodite 口径为机器正则 133/778 = 17.1%；
- 清单/报告的主文件引用与页数对齐。

## P2 · 功能补齐 —— 大部分已执行 ✅，小部分未做 ⏳

| 功能 | 对应教程步骤 | 状态 |
| --- | --- | --- |
| 单位归一化（`--stage normalize`） | 实体规范化与单位统一 | ✅ exact 188 条归一 S/cm |
| 系统化矛盾检测（`--stage contradictions`） | 跨文献知识融合与矛盾检测 | ✅ 85 条候选矛盾对 |
| 报告生成器（`--stage report`） | 报告生成 | ✅ 九节确定性渲染 |
| 检索质量自评估（`--stage retrieval-eval`） | 检索指标 P@K/R@K/nDCG@K | ✅ 规则弱标注版 |
| 精确 P@K/R@K/nDCG@K（人工标注集） | 检索指标 | ⏳ 需 30–50 篇人工标注 |
| MinerU PDF 解析 | 多模态文献解析 | ⏳ 需外部依赖 + PDF 原文 |

## 验收结果（可复跑）

| 检查 | 结果 |
| --- | --- |
| `detect_docid_mismatch.py` | 0 mismatch |
| `--stage audit` | PASS（gaps 19/19、hyps 20/20、routes 12/12） |
| `--stage normalize / contradictions / report / retrieval-eval` | 全部可离线复跑 |

## 自评估措辞（提交时采用）

- 不把"审计 PASS"写成"无误"；如实说明审计校验范围与新增的配对校验。
- 候选 Gap/假设/路线均为**待证伪假设**，不对应湿实验性能保证。
- 矛盾检测为**未对齐测量温度的初筛**，数值仅用于领域聚焦度自评估。