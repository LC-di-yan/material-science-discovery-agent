# 优化与修复增补说明

> 本说明是《初赛方案.pdf》（主提交物，22 页 = 21 页正文 + 1 页本说明摘要附录）的补充件，记录 2026-08-04 提交快照形成后、至 2026-08-15 完成的证据链修复与功能补齐。摘要已追加为该 PDF 第 22 页附录；如需回退，删除末页即可恢复原 21 页正文。

## 一、证据链正确性修复（最高优先级）

**问题**：discover 循环自主生成的 `gap_012–019` 的 `evidence_doc_ids` 存在复用/伪造（多个不同 `record_id` 被填成同一个 `doc_id` `dccfef3859ae…`），连同早期 `gap_001–009` 的 12 位短哈希截断，经 `detect_docid_mismatch.py` 检出 **56 处 mismatch**。修复前 `audit` 因不检查该字段而误判 PASS。

**根因（三连环缺口）**：
1. `read_knowledge` 返回字段不含 `doc_id`，discover 里的 LLM 拿不到正确值，只能瞎填一个"长得像哈希"的串；
2. `write_gap` 只校验 `record_id` 存在，不校验传入 `doc_id` 与 record 的对应；
3. `audit` 只查 `record_id → doc_id` 可回溯性，不对照 gap 自填的 `doc_id`。

**修复**：
- `fix_docid.py` 按 `record_id` 从 `records.jsonl` 反查回填全部真实 `doc_id`（17 个 gap），原始件备份为 `gaps.jsonl.pre_fixdocid`；
- `tools.py`：`read_knowledge` 暴露 `doc_id`；`write_gap` 落盘前按 `record_id` 回填、不再信任调用方传入值；
- `audit.py`：新增 `record_id↔doc_id` 配对一致性校验。

**验收**：`detect_docid_mismatch.py` mismatch = 0；`--stage audit` 判定 PASS。

## 二、口径与可复现对齐

- `evaluate.py`：输出名带快照日期（`evaluation_{snapshot}.json`），已存在则拒绝覆盖（`--force` 解除）。
- `comparator.py`：冻结路线已存在则拒绝覆盖；`audited_corpus` 动态指向最新评估快照。
- 核心 argyrodite 占比统一为机器正则口径 133/778 = **17.1%**（取代早期不一致口径）。
- 调研报告补 §11；清单主文件引用改为 `初赛方案.pdf`。

## 三、功能补齐（均为纯标准库确定性实现、无网络无 LLM）

### 3.1 实体规范化与单位统一（`--stage normalize`）
把 283 种非结构化 `conductivity` 写法规一为统一单位 S/cm 数值（`data/02_knowledge/normalized_conductivity.jsonl`）。确定性解析覆盖科学计数、`×10⁻³` 上标、LaTeX `10^{-3}`、`mS/cm↔S/cm` 换算、缺乘号、不确定度括号；范围/多值/纯描述一律标记不猜测。
**结果**：可归一单值 **188** 条（值域 1e-9 ~ 2e-1 S/cm，中位 1 mS/cm），multi 68、geq 12、approx 13、unparseable 23、empty 473。

### 3.2 跨文献矛盾检测（`--stage contradictions`）
对"同一归一体系内、电导率相差 ≥10 倍"的记录对做确定性成对检测，排除 <1e-7 S/cm 的电子电导/噪声（`data/02_knowledge/contradictions.jsonl`），每条保留双方 `measurement_temp` 供复核。
**结果**：**85 条候选矛盾对、17 个归一体系**，top 集中在 Li6PS5Cl 体系内跨文献 2–3 个数量级分散，把原 `gap_005` 的定性判断量化为可追溯清单。

### 3.3 报告生成器（`--stage report`）
从本地 JSON/JSONL 确定性渲染九节调研报告（`调研报告_auto_20260815.md`），无 LLM、无网络，含语料规模、字段覆盖、归一化统计、Gap/假设/路线、矛盾清单、四臂对照、审计结论与边界声明，证明"报告不是 LLM 自由发挥"。

### 3.4 检索质量自评估（`--stage retrieval-eval`）
从 6 个 `search_*.jsonl` 原始响应恢复 317 个 query 的有序命中，用领域规则做弱相关性分级，计算 P@K / 相对 Recall@K / nDCG@K。
**结果**：P_strong@10=0.658、P_rel@10=0.762、recall_rel@10=0.440、nDCG@10=0.875；tier0 占 34.8% 为检索噪声。
**边界**：规则弱标注 ≠ 人工标注基准，升级路径 = 用 30–50 篇人工标注集替换弱标注。

## 四、验收汇总（可复跑）

| 命令 | 输出 | 结果 |
| --- | --- | --- |
| `python detect_docid_mismatch.py` | mismatch 总数 | 0 |
| `python -m agent.run --stage audit` | `audit_*.json` | PASS（gaps 19/19、hyps 20/20、routes 12/12） |
| `python -m agent.run --stage normalize` | `normalized_conductivity.jsonl` | exact 188 |
| `python -m agent.run --stage contradictions` | `contradictions.jsonl` | 85 对 / 17 体系 |
| `python -m agent.run --stage report --snapshot 20260815` | `调研报告_auto_20260815.md` | 九节可回溯 |
| `python -m agent.run --stage retrieval-eval` | `retrieval_eval_*.json` | nDCG@10=0.875 |

## 五、诚实披露与边界

- 修复前曾存在 56 处 `evidence_doc_ids` 不一致，本说明不掩盖，检测脚本、修复脚本与备份均留痕，可复现复核。
- 所有新功能零运行时依赖、无网络、无 LLM。
- 候选 Gap/假设/路线均为待证伪假设；矛盾检测为未对齐测量温度的初筛，需逐条复核。